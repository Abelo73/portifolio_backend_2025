# App
#spring.application.name=${SPRING_APPLICATION_NAME}
spring.application.name=portifolio_backend

server.port=8080

# Render cloud DB
#spring.datasource.url=${RENDER_DATASOURCE_URL}
#spring.datasource.username=${RENDER_DATASOURCE_USERNAME}
#spring.datasource.password=${RENDER_DATASOURCE_PASSWORD}

#spring.datasource.url=${SPRING_DATASOURCE_URL}
#spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
#spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}

#SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/portfolio
spring.datasource.url=jdbc:postgresql://localhost:5432/portfolio?sslmode=disable
spring.datasource.username=postgres
spring.datasource.password=password




# src/main/resources/application-prod.properties

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect


# Hibernate (same for both environments)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Cloudinary
#cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME}
#cloudinary.api-key=${CLOUDINARY_API_KEY}
#cloudinary.api-secret=${CLOUDINARY_API_SECRET}

cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME}
cloudinary.api-key=${CLOUDINARY_API_KEY}
cloudinary.api-secret=${CLOUDINARY_API_SECRET}

# Frontend URL
frontend.url=${FRONTEND_URL}
#SPRING_PROFILES_ACTIVE=prod
